<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>User Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            background-color: #f8f9fa;
        }

        .profile-photo-wrapper {
            margin-top: 10px;
            /* shifts image slightly down */
            /* you can add more styles here if needed */
        }

        .profile-img {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #dee2e6;
            margin-top: 10px;
            /* shifts image slightly down */
        }
    </style>
</head>

<body>
    <div class="container py-5">
        <div class="card shadow-lg p-4 text-center">
            <!-- Profile Photo Wrapper -->
            <div class="profile-photo-wrapper mb-3">
                <img src="<%= user && user.photo ? user.photo : 'https://cdn-icons-png.flaticon.com/512/847/847969.png' %>"
                    alt="Profile Photo" class="profile-img" />
            </div>

            <!-- Name -->
            <h4 class="mb-2">Welcome, <%= user && (user.displayName || user.username || 'User' ) %>
            </h4>

            <!-- Email -->
            <p class="text-muted mb-1">Email: <%= user && user.email ? user.email : 'Not Available' %>
            </p>

            <!-- Logout Button -->
            <a href="/logout" class="btn btn-danger btn-sm mt-2">Logout</a>
        </div>

    </div>
</body>

</html>